diagram:
  name: Minimal Test
  layout: auto

resources:
  - type: azure/app-service
    name: web-app
  - type: azure/sql-database
    name: prod-db

connections:
  - from: web-app
    to: prod-db
    label: SQL connection
